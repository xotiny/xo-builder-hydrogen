import { useEffect } from 'react';
import { loadStyle } from "../utils/load-style";
export function useStyle(element) {
    useEffect(() => {
        if (process.env.NODE_ENV === 'development') {
            loadStyle({
                id: element.id,
                content: '',
            });
        }
        // eslint-disable-next-line react-hooks/exhaustive-deps
    }, []);
}
