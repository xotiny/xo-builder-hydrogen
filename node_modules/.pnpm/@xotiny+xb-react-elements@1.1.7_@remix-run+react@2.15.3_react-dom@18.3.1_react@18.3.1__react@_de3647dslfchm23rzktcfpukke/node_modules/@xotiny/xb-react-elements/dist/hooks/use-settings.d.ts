import { GlobalSettings, PageSettings } from "../types/settings.entity";
interface XoBuilderSettings {
    pageSettings: PageSettings;
    globalSettings: GlobalSettings;
}
export declare function useSettings(): XoBuilderSettings;
export {};
