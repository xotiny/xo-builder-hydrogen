import { ReactNode } from 'react';
export interface ItemContextValue<T> {
    data: T;
}
export interface ItemProviderProps<T> extends ItemContextValue<T> {
    children: ReactNode;
}
export declare function createShopifyItemContext(): {
    Provider: <T>({ data, children }: ItemProviderProps<T>) => import("react/jsx-runtime").JSX.Element;
    useItem: <T>() => T | null;
};
