export interface LoadScriptInput {
    file?: string;
    content?: string;
    insertPosition?: InsertPosition;
    id?: string;
    parentElement?: HTMLElement;
    type?: string;
    className?: string;
    async?: boolean;
    defer?: boolean;
}
export declare function loadScript({ file, content, insertPosition, id, parentElement, type, className, async, defer, }: LoadScriptInput): Promise<HTMLScriptElement>;
