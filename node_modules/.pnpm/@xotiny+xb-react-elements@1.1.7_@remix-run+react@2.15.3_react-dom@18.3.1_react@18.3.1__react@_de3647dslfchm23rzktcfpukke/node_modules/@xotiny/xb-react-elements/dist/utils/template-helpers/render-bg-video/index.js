import { jsx as _jsx } from "react/jsx-runtime";
import { isEmpty } from "../../is-empty";
export function renderBgVideo({ styles, className } = { styles: {}, className: '' }) {
    if (isEmpty(styles)) {
        return null;
    }
    const bgType = styles?.normal?.background?.['background-type'] ?? 'standard';
    if (bgType !== 'video') {
        return null;
    }
    const videoUrlDesktop = styles?.normal?.background?.['background-video']?.desktop?.url;
    const videoUrlTablet = styles?.normal?.background?.['background-video']?.tablet?.url;
    const videoUrlMobile = styles?.normal?.background?.['background-video']?.mobile?.url;
    const video = videoUrlDesktop || videoUrlTablet || videoUrlMobile;
    const hasBreakpoints = videoUrlTablet || videoUrlMobile;
    let breakpoints = { 'xo-breakpoints': '{}' };
    const breakpointsArr = [];
    let privateClassName = 'xb-bg-video';
    if (className) {
        privateClassName += ` ${className}`;
    }
    if (!video) {
        return null;
    }
    if (videoUrlTablet) {
        breakpointsArr.push(`991: { src: '${videoUrlTablet}' }`);
    }
    if (videoUrlMobile) {
        breakpointsArr.push(`767: { src: '${videoUrlMobile}' }`);
    }
    if (hasBreakpoints) {
        breakpoints = { 'xo-breakpoints': `{${breakpointsArr.join(', ')}}` };
    }
    return _jsx("xo-video-cover", { class: privateClassName, "xo-src": videoUrlDesktop, ...breakpoints });
}
