import { jsx as _jsx } from "react/jsx-runtime";
import { isEmpty } from "../../is-empty";
import { Style } from '../style';
export function renderBgOverlay({ styles, className } = { styles: {}, className: '' }) {
    if (isEmpty(styles)) {
        return null;
    }
    const bgType = styles?.normal?.background?.['background-type'] ?? 'standard';
    if (bgType === 'standard') {
        return null;
    }
    const overlayDesktop = styles?.normal?.background?.['background-overlay']?.desktop;
    const overlayTablet = styles?.normal?.background?.['background-overlay']?.tablet;
    const overlayMobile = styles?.normal?.background?.['background-overlay']?.mobile;
    const overlay = overlayDesktop || overlayTablet || overlayMobile;
    const videoVars = Style.create();
    let privateClassName = 'xb-bg-overlay';
    if (className) {
        privateClassName += ` ${className}`;
    }
    if (!overlay) {
        return null;
    }
    if (overlayDesktop) {
        videoVars.set({ '--overlay-desktop': overlayDesktop });
    }
    if (overlayTablet) {
        videoVars.set({ '--overlay-tablet': overlayTablet });
    }
    if (overlayMobile) {
        videoVars.set({ '--overlay-mobile': overlayMobile });
    }
    return _jsx("div", { className: privateClassName, style: videoVars.get() });
}
