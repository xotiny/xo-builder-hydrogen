import { CSSProperties, HTMLAttributes } from 'react';
export interface Options {
    classNames?: string[];
    style?: CSSProperties & Record<string, any>;
    isCustomElement?: boolean;
}
export declare function elementAttr(element: any, { classNames, style, isCustomElement }: Options, elementClassEnabled?: boolean): HTMLAttributes<HTMLElement>;
