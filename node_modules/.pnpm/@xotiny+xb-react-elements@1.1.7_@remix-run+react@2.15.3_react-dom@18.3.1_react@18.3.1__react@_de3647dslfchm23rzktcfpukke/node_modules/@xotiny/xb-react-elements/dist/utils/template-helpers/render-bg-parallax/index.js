import { jsx as _jsx } from "react/jsx-runtime";
import { isEmpty } from "../../is-empty";
export function renderBgParallax({ styles, className } = { styles: {}, className: '' }) {
    if (isEmpty(styles)) {
        return null;
    }
    const bgType = styles?.normal?.background?.['background-type'] ?? 'standard';
    const bgParallaxEnabled = styles?.normal?.background?.['background-image-parallax'];
    if (bgType !== 'image') {
        return null;
    }
    if (!bgParallaxEnabled) {
        return null;
    }
    const parallaxDesktop = styles?.normal?.background?.['background-image']?.desktop?.url;
    const parallaxTablet = styles?.normal?.background?.['background-image']?.tablet?.url;
    const parallaxMobile = styles?.normal?.background?.['background-image']?.mobile?.url;
    const parallax = parallaxDesktop || parallaxTablet || parallaxMobile;
    const parallaxVars = {};
    let privateClassName = 'xb-bg-parallax';
    if (className) {
        privateClassName += ` ${className}`;
    }
    if (!parallax) {
        return null;
    }
    return (_jsx("div", { className: privateClassName, style: parallaxVars, children: _jsx("xo-parallax", { "xo-lerp-ease": "0.4", class: "xb-bg-parallax__item", children: _jsx("xo-parallax-scroll", { "xo-keyframes": "\n            {\n              '0%': { backgroundPositionY: '200px' },\n              '100%': { backgroundPositionY: '-200px' },\n            }\n          ", class: "xb-bg-parallax__url", children: ' ' }) }) }));
}
