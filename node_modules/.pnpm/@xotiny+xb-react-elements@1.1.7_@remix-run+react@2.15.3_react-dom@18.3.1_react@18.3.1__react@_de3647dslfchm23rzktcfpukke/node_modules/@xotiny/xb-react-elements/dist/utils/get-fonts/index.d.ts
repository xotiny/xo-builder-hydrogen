import { CurrentElementEntity } from "../../types/current-element.entity";
import { ElementsEntity } from "../../types/elements.entity";
export interface InputOverload {
    currentElements: CurrentElementEntity.Elements;
}
export declare function getFonts<T extends Record<string, any>>(data: InputOverload): T[];
export declare function getFonts<T extends Record<string, any>>(settings: Record<string, ElementsEntity.Settings>): T[];
type Variant = {
    value: string;
    weight: string;
    italic: boolean;
};
type Font = {
    family: string;
    variants: Variant[];
};
export declare function generateGoogleFontUrl({ family, variants }: Font): string;
export {};
