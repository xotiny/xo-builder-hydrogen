import { CurrentElementEntity } from "../../types/current-element.entity";
import { ElementsEntity } from "../../types/elements.entity";
export type AllStyles = string[];
export type Merged = Record<string, string[]>;
export declare function getAllStyles(currentElements: CurrentElementEntity.Elements, elements: ElementsEntity.Entities): AllStyles;
