import type { CSSProperties } from 'react';
export declare class Style {
    private style;
    constructor(initial?: CSSProperties & Record<string, any>);
    static create(initial?: CSSProperties & Record<string, any>): Style;
    set(style: CSSProperties & Record<string, any>): void;
    get(): CSSProperties;
    static combine(...styles: Record<string, any>[]): CSSProperties;
}
