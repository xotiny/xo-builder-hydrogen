// import { CONFIG } from 'src/config';
// import { loadScript } from 'src/utils/load-script';
// import { storage } from 'src/utils/storage';
export async function devReload() {
    // await loadScript({ id: 'socket', file: `${CONFIG.dev.baseURL}/socket.io/socket.io.js` });
    // // @ts-ignore
    // const socket = io(CONFIG.dev.baseURL);
    // socket.connect();
    // socket.on('@@development/hydrogen-reload', () => {
    //   storage.setItem('@@development/scrollY', window.scrollY.toString());
    //   window.location.reload();
    //   const scrollY = Number(storage.getItem('@@development/scrollY') ?? '0');
    //   window.scrollTo({ top: scrollY });
    // });
}
