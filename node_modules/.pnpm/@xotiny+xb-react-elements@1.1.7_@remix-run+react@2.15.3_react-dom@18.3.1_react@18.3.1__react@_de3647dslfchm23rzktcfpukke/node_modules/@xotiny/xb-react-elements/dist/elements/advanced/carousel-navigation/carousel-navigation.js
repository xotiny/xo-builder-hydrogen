import { jsx as _jsx } from "react/jsx-runtime";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import { Style } from "../../../utils/template-helpers/style";
import './styles';
export const CarouselNavigation = ({ element, children }) => {
    const { settings } = element;
    const carouselName = settings.carousel_id;
    function getStyle() {
        const style = Style.create({
            '--xb-carousel-direction': `${settings.direction === 'horizontal' ? 'row' : 'column'}`,
            '--xb-carousel-vertical-gap': `${settings.verticalSpacing.value}${settings.verticalSpacing.unit}`,
            '--xb-carousel-horizontal-gap': `${settings.horizontalSpacing.value}${settings.horizontalSpacing.unit}`,
            '--xb-carousel-horizontal-align': settings.horizontalAlign,
            '--xb-carousel-vertical-align': settings.verticalAlign,
        });
        return style.get();
    }
    return (_jsx("div", { ...elementAttr(element, {
            classNames: [
                'xb-carousel-navigation',
                `xb-carousel-navigation--${settings.placement}`,
                `xb-carousel-navigation--${settings.position}`,
                settings.static?.class,
            ],
            style: { ...getStyle() },
        }), "data-carousel-name": carouselName, children: children }));
};
