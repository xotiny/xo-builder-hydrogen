import { jsx as _jsx } from "react/jsx-runtime";
import { useCollection } from "../../../hooks/use-shopify-data";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import './styles';
export const CollectionListItem = ({ element, children }) => {
    const { settings } = element;
    const collection = useCollection(element.id);
    const linkAttr = settings.$linkEnabled === 'yes' ? { 'xb-href': `/collections/${collection?.handle}` } : {};
    const attr = settings.linkNewTab === 'yes' ? { 'xb-target': '_blank' } : {};
    return (_jsx("div", { ...elementAttr(element, {
            classNames: ['xb-collection__item', settings.static?.class],
        }), ...linkAttr, ...attr, children: children }));
};
