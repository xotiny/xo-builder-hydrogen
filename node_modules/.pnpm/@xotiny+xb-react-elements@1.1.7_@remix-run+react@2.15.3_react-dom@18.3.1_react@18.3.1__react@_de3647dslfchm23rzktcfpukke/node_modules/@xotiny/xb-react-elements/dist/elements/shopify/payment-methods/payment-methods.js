import { jsx as _jsx } from "react/jsx-runtime";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import { Style } from "../../../utils/template-helpers/style";
import './styles';
export const PaymentMethods = ({ element, children }) => {
    const { settings } = element;
    function getStyle() {
        const style = Style.create({
            '--xb-payment-methods-spacing': `${settings.itemsSpacing?.value}px`,
        });
        return style.get();
    }
    return (_jsx("div", { ...elementAttr(element, {
            classNames: ['xb-payment-methods', settings.static?.class],
            style: getStyle(),
        }), children: children }));
};
