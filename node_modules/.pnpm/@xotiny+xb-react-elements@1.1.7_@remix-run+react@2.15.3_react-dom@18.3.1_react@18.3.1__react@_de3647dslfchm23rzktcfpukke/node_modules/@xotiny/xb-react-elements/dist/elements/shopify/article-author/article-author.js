import { jsx as _jsx } from "react/jsx-runtime";
import { useArticle } from "../../../hooks/use-shopify-data";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import './styles';
export const ArticleAuthor = ({ element }) => {
    const { settings } = element;
    const article = useArticle(element.id);
    if (!article || !article?.author?.name) {
        return null;
    }
    return (_jsx("div", { ...elementAttr(element, {
            classNames: ['xb-article-author', settings.static?.class],
        }), children: article.author?.name }));
};
