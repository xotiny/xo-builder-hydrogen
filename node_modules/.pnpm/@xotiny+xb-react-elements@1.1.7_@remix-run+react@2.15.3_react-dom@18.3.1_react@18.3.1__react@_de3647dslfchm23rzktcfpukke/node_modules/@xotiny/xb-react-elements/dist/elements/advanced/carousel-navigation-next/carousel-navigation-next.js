import { jsx as _jsx } from "react/jsx-runtime";
import './styles';
export const CarouselNavigationNext = ({ parent, children }) => {
    const carouselName = parent?.settings.carousel_id;
    return (_jsx("xo-carousel-next", { class: "carousel_navigation carousel_navigation--next carousel_navigation--center", onClick: () => {
            if (carouselName) {
                window.xoCarousel.next(carouselName, 1);
            }
        }, children: children }));
};
