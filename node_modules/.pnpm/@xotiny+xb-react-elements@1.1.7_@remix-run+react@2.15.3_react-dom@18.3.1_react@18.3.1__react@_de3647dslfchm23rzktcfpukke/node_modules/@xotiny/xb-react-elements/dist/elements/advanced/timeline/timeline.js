import { jsx as _jsx } from "react/jsx-runtime";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import { Style } from "../../../utils/template-helpers/style";
import './styles';
export const Timeline = ({ element, children }) => {
    const { settings } = element;
    function getStyle() {
        const style = Style.create({
            '--xb-timeline-color': settings.line_color,
        });
        return style.get();
    }
    return (_jsx("div", { ...elementAttr(element, {
            classNames: ['xb-timeline', settings.static?.class, settings?.modifier?.handle],
            style: getStyle(),
        }), children: children }));
};
