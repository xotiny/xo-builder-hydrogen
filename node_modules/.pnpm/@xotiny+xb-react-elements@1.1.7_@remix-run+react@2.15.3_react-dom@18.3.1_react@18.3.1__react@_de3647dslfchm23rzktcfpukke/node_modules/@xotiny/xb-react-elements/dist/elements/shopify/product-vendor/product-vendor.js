import { Fragment as _Fragment, jsxs as _jsxs } from "react/jsx-runtime";
import { createElement } from 'react';
import { useProduct } from "../../../hooks/use-shopify-data";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
export const ProductVendor = ({ element }) => {
    const { settings } = element;
    const actionEnabled = settings.linkToCollectionEnabled === 'yes';
    const tagName = actionEnabled ? 'a' : 'div';
    const classLink = actionEnabled ? 'xb-link' : '';
    const { product } = useProduct(element.id);
    if (!product || !product.vendor) {
        return null;
    }
    return createElement(tagName, {
        ...elementAttr(element, {
            classNames: ['xb-product-vendor', classLink, settings.static?.class],
        }),
        children: (_jsxs(_Fragment, { children: [settings.customEnabled === 'yes' && settings.prefixText, product.vendor, settings.customEnabled === 'yes' && settings.suffixText] })),
    });
};
