import { jsx as _jsx } from "react/jsx-runtime";
import { useElementSettings } from "../../../hooks/use-shopify-data";
import './styles';
export const CollectionFilterFacetsHorizontal = ({ children }) => {
    const parentSettings = useElementSettings();
    if (!parentSettings || parentSettings?.$filterType !== 'horizontal') {
        return null;
    }
    return _jsx("div", { className: "xb-facets-horizontal", children: children });
};
