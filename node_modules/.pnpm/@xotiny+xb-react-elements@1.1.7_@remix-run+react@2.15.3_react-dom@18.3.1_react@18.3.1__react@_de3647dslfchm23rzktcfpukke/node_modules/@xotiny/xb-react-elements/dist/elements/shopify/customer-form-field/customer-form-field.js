import { jsx as _jsx } from "react/jsx-runtime";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import './styles';
export const CustomerFormField = ({ element, children }) => {
    const { settings } = element;
    const style = settings.showLabel === 'yes' && settings.labelPosition === 'left'
        ? {
            display: 'flex',
            columnGap: '10px',
        }
        : {};
    return (_jsx("label", { ...elementAttr(element, {
            classNames: ['xb-customer-form__field', settings.static?.class, settings?.modifier?.handle],
            style,
        }), children: children }));
};
