import { hash } from "../../../utils/hash";
import { Style } from "../../../utils/template-helpers/style";
export function imageComparisonUtils({ element }) {
    const { settings } = element;
    function getStyle() {
        const style = Style.create({
            '--xb-image-comparison-handle-color': settings.handleColor,
        });
        return style.get();
    }
    const placeholderImage = 'https://cdn.shopify.com/s/files/1/0677/7900/2622/files/Default_image.png?v=1713954435';
    const iconColor = settings.handleColor ?? '#ffffff';
    const observed = hash(JSON.stringify(settings));
    return {
        getStyle,
        placeholderImage,
        observed,
        iconColor,
    };
}
