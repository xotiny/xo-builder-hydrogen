import { jsx as _jsx } from "react/jsx-runtime";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import { Style } from "../../../utils/template-helpers/style";
import './styles';
export const TimelineItem = ({ element, children, parent }) => {
    const { settings } = element;
    function getStyle() {
        const style = Style.create({
            '--xb-timeline-color': `${settings.disable_line === 'yes' ? 'transparent' : parent?.settings.line_color}`,
        });
        return style.get();
    }
    const idx = element.index + 1;
    const evenOrOddClass = idx % 2 === 0 ? 'xb-timeline__content--even' : 'xb-timeline__content--odd';
    const firstEleClass = idx === 1 ? 'xb-timeline__content--first' : '';
    return (_jsx("div", { ...elementAttr(element, {
            classNames: ['xb-timeline__content', settings.static?.class, settings?.modifier?.handle, evenOrOddClass, firstEleClass],
            style: getStyle(),
        }), children: children }));
};
