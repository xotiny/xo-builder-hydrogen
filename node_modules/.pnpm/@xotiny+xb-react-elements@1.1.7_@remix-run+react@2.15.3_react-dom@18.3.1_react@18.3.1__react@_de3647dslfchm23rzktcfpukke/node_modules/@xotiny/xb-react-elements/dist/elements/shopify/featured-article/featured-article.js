import { jsx as _jsx } from "react/jsx-runtime";
import { ArticleProvider, useArticle } from "../../../hooks/use-shopify-data";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import './styles';
export const FeaturedArticle = ({ children, element }) => {
    const { settings } = element;
    const article = useArticle(element.id);
    if (!article) {
        return null;
    }
    const stylesVar = {
        '--article-max-line': `${settings.max_line.value}`,
    };
    const hiddenContentClass = settings.content_type === 'short' ? 'xb-featured-article--short' : 'xb-featured-article--full';
    return (_jsx("div", { ...elementAttr(element, {
            classNames: ['xb-featured-article', hiddenContentClass, settings?.static?.class],
            style: stylesVar,
        }), children: _jsx(ArticleProvider, { data: article, children: children }) }));
};
