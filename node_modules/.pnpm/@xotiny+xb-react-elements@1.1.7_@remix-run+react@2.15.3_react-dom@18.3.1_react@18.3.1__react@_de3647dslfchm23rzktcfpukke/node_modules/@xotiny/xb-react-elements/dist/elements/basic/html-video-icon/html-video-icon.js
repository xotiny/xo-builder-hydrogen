import { jsx as _jsx } from "react/jsx-runtime";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import './styles';
export const HtmlVideoIcon = ({ element }) => {
    const { settings } = element;
    return (_jsx("div", { ...elementAttr(element, {
            classNames: [
                'xb-video-icon',
                'xb-html-video__icon',
                `xb-html-video__icon--${settings.iconPosition}`,
                settings.static?.class,
                settings?.modifier?.handle,
            ],
        }), dangerouslySetInnerHTML: {
            __html: settings.icon.type === 'image'
                ? `<img className="xb-image" src="${settings.icon.source}" alt="${settings.icon.name}" />`
                : settings.icon.source,
        } }));
};
