import { createElement } from 'react';
import { elementAttr } from "../../../utils/template-helpers/element-attr";
export const CountdownTime = ({ element }) => {
    const { settings } = element;
    return createElement(settings.type, {
        ...elementAttr(element, {
            classNames: ['xb-countdown__value', settings.static?.class],
        }),
    }, 0);
};
