import { jsx as _jsx } from "react/jsx-runtime";
import { useArticle } from "../../../hooks/use-shopify-data";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
export const ArticleListItem = ({ element, children }) => {
    const { settings } = element;
    const article = useArticle(element.id);
    if (!article) {
        return null;
    }
    const linkAttr = settings.$linkEnabled !== 'no' ? { 'xb-href': `/blogs/${article.blog?.handle}/${article.handle}` } : {};
    const attr = settings.linkNewTab === 'yes' ? { 'xb-target': '_blank' } : {};
    return (_jsx("div", { ...elementAttr(element, {
            classNames: ['xb-article-list__item', settings.static?.class],
        }), ...linkAttr, ...attr, children: children }));
};
