import { WrapperProps } from "../../../types/element-props";
import { ButtonSettings } from './types';
export declare function buttonUtils({ element }: Pick<WrapperProps<ButtonSettings>, 'element'>): {
    fullWidthClass: string;
    tagName: string;
    classLink: string;
    actionEnabled: boolean;
};
