import { jsx as _jsx } from "react/jsx-runtime";
import { hash } from "../../../utils/hash";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import './styles';
export const HotspotContent = ({ element, children, parent }) => {
    const { settings } = element;
    const observed = hash(JSON.stringify(parent?.settings));
    const hotspotPopoverId = `hotspot-${parent?.id}-${observed}`;
    const classPlacement = `xb-hotspot__item-content-${settings.hotspot_information_placement}`;
    return (_jsx("xo-builder-popover-content", { ...elementAttr(element, {
            classNames: ['xb-hotspot__item-content', settings.static?.class, settings?.modifier?.handle, classPlacement],
            isCustomElement: true,
        }), "xo-placement": parent?.settings.hotspot_information_placement, "xo-name": hotspotPopoverId, "xo-portal": true, "xo-easing": parent?.settings.animation_easing, "xo-duration": parent?.settings.visible_duration.value, "xo-animate": parent?.settings.hover_animation, children: _jsx("div", { children: children }) }));
};
