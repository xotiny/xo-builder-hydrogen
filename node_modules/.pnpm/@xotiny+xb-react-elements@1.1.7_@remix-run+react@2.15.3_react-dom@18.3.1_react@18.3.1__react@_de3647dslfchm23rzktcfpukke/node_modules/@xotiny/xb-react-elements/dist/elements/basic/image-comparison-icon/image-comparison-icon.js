import { jsx as _jsx } from "react/jsx-runtime";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import './styles';
export const ImageComparisonIcon = ({ element }) => {
    const { settings } = element;
    return (_jsx("xo-image-comparison-handle-icon", { ...elementAttr(element, {
            classNames: ['xb-image-comparison__icon', settings.static?.class, settings?.modifier?.handle],
            isCustomElement: true,
        }), dangerouslySetInnerHTML: {
            __html: settings.icon.type === 'image'
                ? `<img class="xb-image" src="${settings.icon.source}" alt="${settings.icon.name}" />`
                : settings.icon.source,
        } }));
};
