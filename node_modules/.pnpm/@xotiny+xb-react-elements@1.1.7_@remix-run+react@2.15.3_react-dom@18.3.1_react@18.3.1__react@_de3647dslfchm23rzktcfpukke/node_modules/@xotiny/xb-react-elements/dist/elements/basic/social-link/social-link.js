import { jsx as _jsx } from "react/jsx-runtime";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import { Style } from "../../../utils/template-helpers/style";
import './styles';
export const SocialLink = ({ element, children }) => {
    const { settings } = element;
    function getSocialLinkStyle() {
        const socialLinkStyle = Style.create({
            '--xb-social-spacing': `${settings.spacing.value}px`,
            '--xb-social-align': settings.contentAlignment,
        });
        return socialLinkStyle.get();
    }
    return (_jsx("div", { ...elementAttr(element, {
            classNames: ['xb-social-link', settings?.modifier?.handle, settings.static?.class],
            style: {
                ...getSocialLinkStyle(),
            },
        }), children: children }));
};
