import { jsx as _jsx } from "react/jsx-runtime";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import { Style } from "../../../utils/template-helpers/style";
import './styles';
export const Callout = ({ element, children }) => {
    const { settings } = element;
    function getGap() {
        const gap = Style.create({
            '--xb-callout-gap': `${settings.iconSpacing.value}px`,
        });
        return gap.get();
    }
    return (_jsx("div", { ...elementAttr(element, {
            classNames: ['xb-callout', settings.static?.class],
            style: getGap(),
        }), children: children }));
};
