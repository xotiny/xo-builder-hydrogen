import { jsx as _jsx } from "react/jsx-runtime";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
export const AccordionItem = ({ element, children, parent }) => {
    const { settings } = element;
    const modifierClass = parent?.settings?.modifier?.handle ? `${parent?.settings?.modifier?.handle}__item` : '';
    return (_jsx("div", { ...elementAttr(element, {
            classNames: ['xb-accordion__item', modifierClass, settings.static?.class],
        }), children: children }));
};
