import { jsx as _jsx } from "react/jsx-runtime";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import { Style } from "../../../utils/template-helpers/style";
import './styles';
export const Share = ({ element, children }) => {
    const { settings } = element;
    function getShareStyle() {
        const shareStyle = Style.create({
            '--xb-social-spacing': `${settings.spacing.value}px`,
            '--xb-social-align': settings.contentAlignment,
        });
        return shareStyle.get();
    }
    return (_jsx("div", { ...elementAttr(element, {
            classNames: ['xb-share', settings?.modifier?.handle, settings.static?.class],
            style: {
                ...getShareStyle(),
            },
        }), children: children }));
};
