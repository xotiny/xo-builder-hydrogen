import { jsx as _jsx } from "react/jsx-runtime";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import './styles';
export const BadgeWrapper = ({ element, children }) => {
    const { settings } = element;
    return (_jsx("div", { ...elementAttr(element, {
            classNames: [
                'xb-badge-wrapper',
                `xb-badge-wrapper--${settings.badgePosition}`,
                `xb-badge-wrapper--${settings.badgeDirection}`,
                settings.static?.class,
            ],
        }), children: children }));
};
