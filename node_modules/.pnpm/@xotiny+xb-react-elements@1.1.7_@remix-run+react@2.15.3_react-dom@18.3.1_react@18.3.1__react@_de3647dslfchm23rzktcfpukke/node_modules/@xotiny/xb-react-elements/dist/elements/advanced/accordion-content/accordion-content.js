import { jsx as _jsx } from "react/jsx-runtime";
import { hash } from "../../../utils/hash";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
export const AccordionContent = ({ element, children, parent }) => {
    const { settings } = element;
    const active = parent?.settings?.defaultActiveEnabled === 'yes' ? { 'xo-active': '' } : {};
    return (_jsx("xo-collapse", { ...elementAttr(element, {
            classNames: [settings.static?.class],
            isCustomElement: true,
        }), "xo-name": parent?.id, "xo-observed": hash(JSON.stringify(parent?.settings)), "xo-duration": parent?.settings?.speed?.value ?? 400, ...active, children: _jsx("div", { className: "xb-accordion__content", children: children }) }));
};
