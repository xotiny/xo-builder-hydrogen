import { jsx as _jsx } from "react/jsx-runtime";
import { useCollection } from "../../../hooks/use-shopify-data";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import './styles';
export const CollectionDescription = ({ element }) => {
    const { settings } = element;
    const collection = useCollection(element.id);
    if (!collection || !collection.description) {
        return null;
    }
    return (_jsx("div", { ...elementAttr(element, {
            classNames: ['xb-collection-description', settings.static?.class],
        }), dangerouslySetInnerHTML: { __html: collection.description } }));
};
