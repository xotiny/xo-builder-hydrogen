import { jsx as _jsx } from "react/jsx-runtime";
import { useProduct } from "../../../hooks/use-shopify-data";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import './styles';
export const ProductPriceWrapper = ({ element, children }) => {
    const { settings } = element;
    const { product } = useProduct(element.id);
    if (!product) {
        return null;
    }
    return (_jsx("div", { children: _jsx("div", { ...elementAttr(element, {
                classNames: ['xb-product-price-wrapper', `xb-product-price-wrapper--${settings.priceDirection}`, settings.static?.class],
                style: settings.priceDirection === 'row'
                    ? {
                        justifyContent: settings.priceAlignment,
                    }
                    : {
                        alignItems: settings.priceAlignment,
                    },
            }), children: children }) }));
};
