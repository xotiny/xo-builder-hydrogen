import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useElementSettings } from "../../../hooks/use-shopify-data";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
export const CollectionFilterFacetsDrawerTrigger = ({ element, children, parent }) => {
    const { settings } = element;
    const parentSettings = useElementSettings();
    const condition = (parentSettings.$filterEnabled == 'yes' && parentSettings.$sortEnabled == 'yes') || parentSettings.$filterEnabled == 'yes';
    return (_jsxs("xo-modal-trigger", { "xo-name": parent?.id, ...elementAttr(element, {
            classNames: ['xb-facets-drawer__trigger', settings?.modifier?.handle, settings.static?.class],
            isCustomElement: true,
        }), children: [children, _jsx("span", { children: condition ? 'Filter' : parentSettings.$sortEnabled == 'yes' ? 'Sort' : '' })] }));
};
