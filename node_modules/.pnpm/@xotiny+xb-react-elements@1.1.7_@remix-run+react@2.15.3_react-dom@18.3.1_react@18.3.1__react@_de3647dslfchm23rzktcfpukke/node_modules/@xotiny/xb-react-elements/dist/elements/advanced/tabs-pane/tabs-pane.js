import { jsx as _jsx } from "react/jsx-runtime";
import { hash } from "../../../utils/hash";
import { classNames } from "../../../utils/class-names";
import './styles';
export const TabsPane = ({ element, children, parent }) => {
    const { settings } = element;
    const observed = hash(JSON.stringify(settings));
    const childrenId = parent?.settings.childrenId || (element.index === 0 ? element.id : '');
    const active = element.id === childrenId;
    return (_jsx("xo-tabs-pane", { "xo-name": element.id, "xo-observed": observed, "xo-active": active, class: classNames('xb-tabs-pane', element.id, settings?.styles?.handle), children: children }));
};
