import { jsx as _jsx } from "react/jsx-runtime";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import { useProduct } from "../../../hooks/use-shopify-data";
import './styles';
export const ProductListItem = ({ element, children }) => {
    const { settings } = element;
    const { product } = useProduct(element.id);
    const linkAttr = settings.$linkEnabled === 'yes' ? { 'xb-href': `/products/${product?.handle}` } : {};
    const attr = settings.linkNewTab === 'yes' ? { 'xb-target': '_blank' } : {};
    return (_jsx("div", { ...elementAttr(element, {
            classNames: ['xb-product-list__item', settings.static?.class],
        }), ...linkAttr, ...attr, children: children }));
};
