import { jsx as _jsx } from "react/jsx-runtime";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import { hash } from "../../../utils/hash";
import './styles';
export const Accordion = ({ element, children }) => {
    const { settings } = element;
    if (settings.multipleEnabled === 'yes') {
        return (_jsx("div", { ...elementAttr(element, {
                classNames: ['xb-accordion', settings?.modifier?.handle, settings.static?.class],
            }), "xo-observed": hash(JSON.stringify(settings)), children: children }));
    }
    return (_jsx("xo-collapse-provider", { ...elementAttr(element, {
            classNames: ['xb-accordion', settings?.modifier?.handle, settings.static?.class],
            isCustomElement: true,
        }), "xo-observed": hash(JSON.stringify(settings)), children: children }));
};
