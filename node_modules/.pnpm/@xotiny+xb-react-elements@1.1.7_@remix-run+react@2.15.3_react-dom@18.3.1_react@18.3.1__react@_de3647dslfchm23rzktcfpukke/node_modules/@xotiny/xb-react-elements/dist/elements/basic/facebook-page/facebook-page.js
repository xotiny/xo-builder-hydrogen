import { jsx as _jsx } from "react/jsx-runtime";
export const FacebookPage = ({ element }) => {
    const { settings } = element;
    const width = settings.width.value;
    const height = settings.height.value;
    const href = settings.href ? encodeURIComponent(settings.href) : '';
    const appId = settings.appId || '';
    const small_header = settings.small_header || false;
    const hide_cover = settings.hide_cover || false;
    return (_jsx("div", { className: "xb-facebook-page", children: _jsx("iframe", { src: `https://www.facebook.com/plugins/page.php?href=${href}&tabs=timeline&width=${width}&height=${height}&small_header=${small_header}&hide_cover=${hide_cover}&appId=${appId}`, width: width, height: height, style: { border: 'none', overflow: 'hidden' }, allowFullScreen: true, allow: "autoplay; clipboard-write; encrypted-media; picture-in-picture; web-share", title: "Facebook Page" }) }));
};
