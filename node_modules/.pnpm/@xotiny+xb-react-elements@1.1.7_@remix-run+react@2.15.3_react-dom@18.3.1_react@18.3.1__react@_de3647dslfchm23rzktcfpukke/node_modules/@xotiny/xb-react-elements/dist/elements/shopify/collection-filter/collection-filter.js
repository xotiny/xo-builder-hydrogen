import { jsx as _jsx } from "react/jsx-runtime";
import { ElementSettingsProvider, useCollectionDetail } from "../../../hooks/use-shopify-data";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import './styles';
export const CollectionFilter = ({ element, children }) => {
    const { settings } = element;
    const data = useCollectionDetail();
    if (!data) {
        return;
    }
    const filterClass = `xb-collection-products-filter--${settings.$filterType}`;
    const filterSidebarClass = `xb-collection-products-filter--sidebar-${settings.$filterEnabled == 'yes' ? 'visible' : 'hidden'}`;
    return (_jsx("div", { ...elementAttr(element, {
            classNames: ['xb-collection-products-filter', filterClass, filterSidebarClass, settings.static?.class],
        }), children: _jsx(ElementSettingsProvider, { data: settings, children: children }) }));
};
