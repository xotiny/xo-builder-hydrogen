import { jsx as _jsx, Fragment as _Fragment, jsxs as _jsxs } from "react/jsx-runtime";
import { createElement } from 'react';
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import { counterUtils } from './counter-utils';
import './styles';
export const Counter = ({ element }) => {
    const { settings } = element;
    const { observed, tagName, titleClass } = counterUtils({ element });
    return createElement(tagName, {
        ...elementAttr(element, {
            classNames: ['xb-counter', titleClass, settings.static?.class, settings?.modifier?.handle],
        }),
        children: (_jsxs(_Fragment, { children: [settings.prefixText, _jsx("xo-countto", { class: "xb-counter__value", "xo-name": element.id, "xo-observed": observed, "xo-from": settings.count_from.value, "xo-to": settings.count_to.value, "xo-duration": settings.count_duration.value, "xo-delay": settings.count_delay.value, children: "0" }), settings.suffixText] })),
    });
};
