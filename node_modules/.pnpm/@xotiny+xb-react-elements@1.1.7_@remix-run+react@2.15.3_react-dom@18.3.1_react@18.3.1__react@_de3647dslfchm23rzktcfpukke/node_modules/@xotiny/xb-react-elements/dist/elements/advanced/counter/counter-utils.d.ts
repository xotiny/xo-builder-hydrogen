import { ElementProps } from "../../../types/element-props";
import { CounterSettings } from './types';
export declare function counterUtils({ element }: Pick<ElementProps<CounterSettings>, 'element'>): {
    observed: number;
    tagName: any;
    titleClass: string;
};
