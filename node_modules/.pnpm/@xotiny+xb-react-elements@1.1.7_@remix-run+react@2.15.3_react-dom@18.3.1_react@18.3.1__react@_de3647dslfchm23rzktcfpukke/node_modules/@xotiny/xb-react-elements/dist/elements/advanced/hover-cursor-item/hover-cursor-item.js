import { jsx as _jsx } from "react/jsx-runtime";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import { Style } from "../../../utils/template-helpers/style";
import './styles';
export const HoverCursorItem = ({ element, children, parent }) => {
    const { settings } = element;
    function getCssVars() {
        const width = settings.width_option === 'auto' ? 'auto' : `${settings.width.value}px`;
        const height = settings.height_option === 'auto' ? 'auto' : `${settings.height.value}px`;
        const style = Style.create({
            '--xb-hover-cursor-width': width,
            '--xb-hover-cursor-height': height,
        });
        return style.get();
    }
    const id = parent?.id;
    return (_jsx("xo-cursor-item", { ...elementAttr(element, {
            classNames: ['xb-hover-cursor', settings.static?.class],
            style: { ...getCssVars() },
            isCustomElement: true,
        }), "xo-name": id, "xo-lerp-ease": settings.lerp_ease.value, children: children }));
};
