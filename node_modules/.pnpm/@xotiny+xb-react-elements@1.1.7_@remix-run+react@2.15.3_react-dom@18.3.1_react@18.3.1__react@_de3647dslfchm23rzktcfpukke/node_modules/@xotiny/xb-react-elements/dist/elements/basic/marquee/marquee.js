import { jsx as _jsx } from "react/jsx-runtime";
import { hash } from "../../../utils/hash";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import './styles';
export const Marquee = ({ element, child, children }) => {
    const { settings } = element;
    const observed = hash(JSON.stringify(settings) + JSON.stringify(child));
    return (_jsx("div", { ...elementAttr(element, {
            classNames: ['xb-marquee', settings.static?.class],
        }), children: _jsx("xo-marquee", { "xo-observed": observed, "xo-speed": settings.speed.value, "xo-rtl": settings.rightToLeft === 'yes', "xo-pause-on-hover": settings.pauseOnHover === 'yes', children: children }) }));
};
