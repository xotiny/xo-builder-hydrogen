import { Fragment as _Fragment, jsx as _jsx } from "react/jsx-runtime";
import { hash } from "../../../utils/hash";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import { articleListUtils } from './article-list-utils';
import './styles';
export const ArticleList = ({ element, children, child }) => {
    const { settings } = element;
    const { autoplayAttr, colDesktop, colTablet, colMobile, gapDesktop, gapMobile, gapTablet, layoutClass } = articleListUtils({ element });
    const observed = hash(`${JSON.stringify(element.settings)}${child.length}`);
    return (_jsx("div", { ...elementAttr(element, {
            classNames: ['xb-article-list', settings.static?.class, settings?.modifier?.handle, layoutClass],
        }), children: element.settings.$layoutType === 'grid' ? (_jsx(_Fragment, { children: children })) : (_jsx("xo-carousel", { "xo-name": element.id, "xo-observed": observed, "xo-per-view": colDesktop, "xo-per-move": settings.sliderPerMove.value, "xo-gap": gapDesktop, "xo-rewind": settings.sliderRewind === 'yes', ...autoplayAttr, "xo-render-bullet": "<div class='pagination__inner'> <span class='pagination__inner-content'></span> </div>", "xo-breakpoints": `
            {
              768: {
                perView: ${colMobile},
                gap: ${gapMobile}
              },
              992: {
                perView: ${colTablet},
                gap: ${gapTablet}
              }
            }
          `, style: {
                '--xo-per-view': `${colDesktop}`,
                '--xo-gap': `${gapDesktop}px`,
            }, children: children })) }));
};
