import { jsxs as _jsxs } from "react/jsx-runtime";
import { useProduct } from "../../../hooks/use-shopify-data";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
export const StockCounter = ({ element }) => {
    const { settings } = element;
    const { product, selectedVariant } = useProduct(element.id);
    if (!product || !selectedVariant) {
        return null;
    }
    return (_jsxs("div", { ...elementAttr(element, {
            classNames: ['xb-stock-counter', settings.static?.class],
        }), children: [selectedVariant.quantityAvailable ?? product.totalInventory, " in stock"] }));
};
