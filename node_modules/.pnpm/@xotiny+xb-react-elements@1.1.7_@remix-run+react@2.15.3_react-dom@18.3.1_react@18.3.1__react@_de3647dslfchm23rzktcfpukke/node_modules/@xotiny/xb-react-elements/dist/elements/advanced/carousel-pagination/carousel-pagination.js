import { jsx as _jsx } from "react/jsx-runtime";
import { Style } from "../../../utils/template-helpers/style";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import './styles';
export const CarouselPagination = ({ element, children }) => {
    const { settings } = element;
    function getStyle() {
        const style = Style.create({
            '--xb-pagination-gap': `${settings.horizontalSpacing.value}${settings.horizontalSpacing.unit}`,
            '--xb-pagination-align': settings.horizontalAlign,
        });
        return style.get();
    }
    return (_jsx("xo-carousel-pagination", { ...elementAttr(element, {
            classNames: [
                'xb-carousel-pagination',
                `xb-carousel-pagination--${settings.placement}`,
                `xb-carousel-pagination--${settings.position}`,
                settings.static?.class,
                settings?.modifier?.handle,
            ],
            style: {
                ...getStyle(),
            },
            isCustomElement: true,
        }), children: children }));
};
