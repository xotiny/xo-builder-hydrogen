import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useCollectionDetail } from "../../../hooks/use-shopify-data";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import './styles';
export const CollectionProducts = ({ element, children }) => {
    const { settings } = element;
    const data = useCollectionDetail();
    if (!data) {
        return null;
    }
    const { collection } = data;
    if (!collection) {
        return null;
    }
    return (_jsx("div", { ...elementAttr(element, {
            classNames: ['xb-collection-products', settings.static?.class],
        }), children: collection?.products?.nodes?.length ? (children) : (_jsxs("div", { className: "xb-collection-products__empty", children: [_jsx("h3", { className: "xb-collection-products__empty-title", children: "No products found" }), _jsxs("div", { className: "xb-collection-products__empty-description", children: ["Use fewer filters or", ' ', _jsx("a", { className: "xb-link", href: `/collections/${collection.handle}`, children: "remove all" })] })] })) }));
};
