import { jsx as _jsx } from "react/jsx-runtime";
import './styles';
export const CarouselNavigationPrev = ({ parent, children }) => {
    const carouselName = parent?.settings.carousel_id;
    return (_jsx("xo-carousel-prev", { class: "carousel_navigation carousel_navigation--prev carousel_navigation--center", onClick: () => {
            if (carouselName) {
                window.xoCarousel.prev(carouselName, 1);
            }
        }, children: children }));
};
