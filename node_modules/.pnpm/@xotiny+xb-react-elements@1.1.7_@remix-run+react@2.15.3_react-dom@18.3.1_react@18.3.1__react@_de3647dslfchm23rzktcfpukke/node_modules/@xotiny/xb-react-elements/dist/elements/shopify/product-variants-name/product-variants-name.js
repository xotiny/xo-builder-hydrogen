import { jsxs as _jsxs } from "react/jsx-runtime";
import { useProductOption } from "../../../hooks/use-shopify-data";
import { elementAttr } from "../../../utils/template-helpers/element-attr";
import './styles';
export const ProductVariantsName = ({ element }) => {
    const { settings } = element;
    const option = useProductOption();
    if (!option) {
        return null;
    }
    return (_jsxs("div", { ...elementAttr(element, {
            classNames: ['xb-product-variant__label', settings.static?.class],
        }), children: [option.name, ":"] }));
};
