import { FC, ReactNode } from 'react';
import { UniqueId } from "../types/shared-kernel";
import { CurrentElementEntity } from "../types/current-element.entity";
export interface MegaMenuProps {
    elementId: UniqueId;
    renderContent: (section: CurrentElementEntity.WrapperElement, index: number, megaMenu?: boolean) => ReactNode;
}
export declare const MegaMenu: FC<MegaMenuProps>;
