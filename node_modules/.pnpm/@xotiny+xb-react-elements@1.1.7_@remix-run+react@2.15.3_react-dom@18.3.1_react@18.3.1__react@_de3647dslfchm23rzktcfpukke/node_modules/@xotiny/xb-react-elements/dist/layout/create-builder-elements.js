export function createBuilderElements(elements) {
    return Object.entries(elements).reduce((acc, [key, value]) => {
        const newKey = key.replace(/[A-Z]/g, letter => `-${letter.toLowerCase()}`).replace(/^-/g, '');
        return {
            ...acc,
            [newKey]: value,
        };
    }, {});
}
